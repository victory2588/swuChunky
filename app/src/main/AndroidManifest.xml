<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.java.swuchunky">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name="userAccount"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwuChunky">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".selectuser"/>
        <activity android:name=".wizard_signup"/>
        <activity android:name=".wizard_login"/>
        <activity android:name=".wizardMainActivity"/>
        <activity android:name=".dobiMainActivity"/>
        <activity android:name=".wizard_reserve1_sel"/>

    </application>

</manifest>